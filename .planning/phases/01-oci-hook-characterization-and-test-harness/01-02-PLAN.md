---
phase: 01-oci-hook-characterization-and-test-harness
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [test/dns_oci_mock.sh, dnsapi/dns_oci.sh]
autonomous: true
requirements: [TEST-01]
user_setup: []
must_haves:
  truths:
    - D-06 tests exercise dns_oci_add and dns_oci_rm through the public DNS hook path.
    - D-08 core-plus-edge proof matrix exists for zone and TXT payload behavior.
    - D-09 parent zone, delegated subzone, parent fallback, no-zone failure, and add/remove symmetry are covered.
    - The known _signed_request return-field parse artifact is characterized and fixed.
  artifacts:
    - test/dns_oci_mock.sh
    - dnsapi/dns_oci.sh
  key_links:
    - Zone tests assert `_signed_request "GET" "/20180115/zones/$candidate" "" "id"` ordering.
    - TXT tests assert PATCH bodies sent to `/20180115/zones/${_domain}/records`.
---

<objective>
Add characterization tests for current OCI zone discovery, TXT add/remove payloads, and the verified return-field parse artifact.

Purpose: Lock down the behavior later delegated-zone changes must preserve or deliberately update.
Output: Passing mocked zone/TXT tests plus a minimal `dnsapi/dns_oci.sh` fix for the stray return-field `)` artifact.
</objective>

<threat_model>
T-01-03: Mocked TXT tests could pass by asserting private helper state instead of the public hook contract.
Mitigation: Drive all record mutation cases through `dns_oci_add` and `dns_oci_rm`, then assert captured `_signed_request` calls.

T-01-04: Fixing the return-field artifact could accidentally change request signing.
Mitigation: Change only the response sanitation assignment in the `_return_field` branch and verify through a focused test plus existing mocked add/remove cases.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-CONTEXT.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-RESEARCH.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-PATTERNS.md
@test/dns_oci_mock.sh
@dnsapi/dns_oci.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zone discovery and TXT payload cases</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/01-oci-hook-characterization-and-test-harness/01-CONTEXT.md</read_first>
  <action>Add cases named `le_test_oci_parent_zone_add`, `le_test_oci_delegated_zone_add`, `le_test_oci_parent_fallback`, `le_test_oci_no_zone_failure`, and `le_test_oci_add_remove_symmetry`. Use `MOCK_OCI_ZONES` to decide which `GET /20180115/zones/$candidate` calls return a dummy id. For add/remove calls, assert the captured PATCH target is `/20180115/zones/${selected_zone}/records`, the body contains `"rtype":"TXT"`, the expected `"rdata":"<value>"`, and the expected `"operation":"ADD"` or `"operation":"REMOVE"`. The no-zone case must assert no PATCH request was captured.</action>
  <verify>CASE=le_test_oci_parent_zone_add,le_test_oci_delegated_zone_add,le_test_oci_parent_fallback,le_test_oci_no_zone_failure,le_test_oci_add_remove_symmetry sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - Parent-zone case proves `_acme-challenge.www.example.com` selects `example.com` when only `example.com` is accessible.
    - Delegated-zone case proves `_acme-challenge.www.dev.example.com` selects `dev.example.com` when both `dev.example.com` and `example.com` are accessible.
    - Parent-fallback case proves `_acme-challenge.www.dev.example.com` selects `example.com` when `dev.example.com` is inaccessible.
    - No-zone case exits through failure and captures no PATCH request.
    - Add/remove symmetry case proves ADD and REMOVE use the same domain and zone target for the same FQDN/value pair.
  </acceptance_criteria>
  <done>Core-plus-edge zone and TXT behavior is captured through public hook entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Characterize and fix return-field parse artifact</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</read_first>
  <action>Add `le_test_oci_signed_request_return_field` that stubs `_get`, `_fingerprint`, `_sign`, and `_mktemp` narrowly enough to call `_signed_request "GET" "/20180115/zones/example.com" "" "id"` with a JSON response of `{"id":"ocid1.dns-zone.oc1..example"}`. First verify the case exposes the current stray trailing `)` artifact if present. Then edit `dnsapi/dns_oci.sh` so the response sanitation line is exactly `_response="$(echo "$_response" | sed 's/\\\"//g')"` with no literal `)` appended after the command substitution. Keep the fix limited to that line.</action>
  <verify>CASE=le_test_oci_signed_request_return_field sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `dnsapi/dns_oci.sh` no longer contains `sed 's/\\\"//g'))`.
    - `CASE=le_test_oci_signed_request_return_field sh test/dns_oci_mock.sh` exits 0.
    - The return-field case asserts the output is exactly `ocid1.dns-zone.oc1..example`, not `ocid1.dns-zone.oc1..example)`.
    - No other `_signed_request` signing, header, method, host, or body logic is changed.
  </acceptance_criteria>
  <done>Verified parser artifact is fixed under a focused test.</done>
</task>

<task type="auto">
  <name>Task 3: Run full zone suite and lint touched shell files</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .github/workflows/shellcheck.yml</read_first>
  <action>Run the full mocked harness and ShellCheck against touched shell files. If `shfmt` is available locally, run `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh` and confirm `git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh` is clean after formatting. If `shfmt` is not available, record that in the plan summary and do not install a new formatter dependency during this phase.</action>
  <verify>sh test/dns_oci_mock.sh && shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh</verify>
  <acceptance_criteria>
    - `sh test/dns_oci_mock.sh` exits 0.
    - `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh` exits 0.
    - If shfmt exists, `git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh` exits 0 after formatting.
    - If shfmt is absent, the summary says local shfmt was absent and cites Phase 5 for formatter pin cleanup.
  </acceptance_criteria>
  <done>Zone/TXT harness is green and touched shell files pass available static checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_parent_zone_add,le_test_oci_delegated_zone_add,le_test_oci_parent_fallback,le_test_oci_no_zone_failure,le_test_oci_add_remove_symmetry sh test/dns_oci_mock.sh`
- [ ] `CASE=le_test_oci_signed_request_return_field sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`
</verification>

<success_criteria>
- TEST-01 is represented by executable mocked parent, delegated, fallback, no-zone, and add/remove symmetry cases.
- The harness proves record mutation through `dns_oci_add` and `dns_oci_rm`, not by calling only private helpers.
- The verified return-field artifact in `dnsapi/dns_oci.sh` is fixed with a minimal patch and a focused test.
- D-06, D-08, and D-09 are visible in the implemented tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oci-hook-characterization-and-test-harness/01-02-SUMMARY.md`.
</output>

---
phase: 01-oci-hook-characterization-and-test-harness
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [test/dns_oci_mock.sh]
autonomous: true
requirements: [TEST-02]
user_setup: []
must_haves:
  truths:
    - D-04 live OCI DNS smoke remains outside the automated Phase 1 gate.
    - D-07 HTTP-boundary testing is avoided except for the focused parse artifact case already covered in 01-02.
    - D-10 auth/security cases cover API-key auth, missing auth, resource-principal current state, and secure-log boundaries.
    - D-11 live proof remains acmetest-compatible and deferred to UAT/final signoff.
  artifacts:
    - test/dns_oci_mock.sh
  key_links:
    - Auth tests run through _oci_config via dns_oci_add or _get_oci_zone.
    - Secure-log tests compare normal debug capture against secure debug capture.
---

<objective>
Add auth-branch fixtures and secure-debug assertions to the OCI hook harness.

Purpose: Prove authentication branch behavior without reading real OCI config, tokens, or private keys, and leave a clear executable fixture for later resource-principal implementation.
Output: Passing API-key, missing-auth, resource-principal-current-state, and secure-debug harness cases.
</objective>

<threat_model>
T-01-05: Dummy secret material leaks through normal debug assertions and masks a real logging problem later.
Mitigation: Capture normal debug and secure debug separately; assert normal debug never contains dummy key, RPST, `Authorization:`, or `ST$` markers.

T-01-06: Resource-principal support appears tested even though Phase 1 does not implement it.
Mitigation: Name the Phase 1 case `le_test_oci_auth_resource_principal_current_state`, assert the current fallback absence explicitly, and include acceptance criteria requiring Phase 3/4 to flip this fixture when fallback is implemented.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-CONTEXT.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-RESEARCH.md
@test/dns_oci_mock.sh
@dnsapi/dns_oci.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add key-based and missing-auth cases</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/REQUIREMENTS.md</read_first>
  <action>Add `le_test_oci_auth_api_key` and `le_test_oci_auth_missing`. The API-key case must prove dummy `OCI_CLI_TENANCY`, `OCI_CLI_USER`, `OCI_CLI_REGION`, and multi-line `OCI_CLI_KEY` let `dns_oci_add` reach a mocked PATCH request without reading `$HOME/.oci/config`. It must assert account persistence captures include only key-based `OCI_CLI_*` names. The missing-auth case must unset all key-based and resource-principal variables, run through `dns_oci_add`, assert failure, assert an error mentions missing key material, and assert no PATCH request was captured.</action>
  <verify>CASE=le_test_oci_auth_api_key,le_test_oci_auth_missing sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `le_test_oci_auth_api_key` exits 0 and captures a PATCH request.
    - `le_test_oci_auth_api_key` does not read a real OCI config path or key file.
    - `le_test_oci_auth_missing` exits 0 by asserting the hook operation fails.
    - Missing-auth assertions prove no PATCH request occurred after auth failure.
  </acceptance_criteria>
  <done>API-key and missing-auth branches are executable without real OCI state.</done>
</task>

<task type="auto">
  <name>Task 2: Add resource-principal current-state fixture</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/01-oci-hook-characterization-and-test-harness/01-RESEARCH.md</read_first>
  <action>Add `le_test_oci_auth_resource_principal_current_state`. The case must set dummy `OCI_RESOURCE_PRINCIPAL_VERSION=2.2`, `OCI_RESOURCE_PRINCIPAL_RPST=/tmp/nonexistent-rpst`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM=/tmp/nonexistent-private.pem`, and `OCI_RESOURCE_PRINCIPAL_REGION=us-ashburn-1` while leaving key-based `OCI_CLI_*` auth unavailable. In Phase 1, assert the current hook fails before PATCH because resource-principal fallback is not implemented yet. Include a clear comment in the test saying Phase 3/4 must change this fixture from current-failure characterization to fallback-success proof when resource principal auth is added.</action>
  <verify>CASE=le_test_oci_auth_resource_principal_current_state sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - The resource-principal current-state case exits 0 by asserting the current operation fails without key-based auth.
    - The case contains the exact strings `OCI_RESOURCE_PRINCIPAL_VERSION`, `OCI_RESOURCE_PRINCIPAL_RPST`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM`, and `OCI_RESOURCE_PRINCIPAL_REGION`.
    - The case asserts no resource-principal token or private key path is saved through `_saveaccountconf_mutable`.
    - A comment names Phase 3/4 as the point where this fixture must become fallback-success proof.
  </acceptance_criteria>
  <done>Resource-principal fixture exists without falsely claiming fallback support is implemented in Phase 1.</done>
</task>

<task type="auto">
  <name>Task 3: Add secure-debug boundary assertions</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/codebase/CONVENTIONS.md</read_first>
  <action>Add `le_test_oci_secure_debug_boundaries`. Use dummy values `TEST_DUMMY_PRIVATE_KEY`, `TEST_DUMMY_RPST`, `Authorization: Signature`, and `ST$TEST_DUMMY_RPST` in controlled helper calls or fixture captures. Assert `MOCK_DEBUG_LOG` does not contain those values and `MOCK_SECURE_DEBUG_LOG` captures sensitive values only when `_secure_debug*` helpers are called. Do not enable `OUTPUT_INSECURE` and do not print secret dummy values to stdout on success.</action>
  <verify>CASE=le_test_oci_secure_debug_boundaries sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - Normal debug capture does not contain `TEST_DUMMY_PRIVATE_KEY`.
    - Normal debug capture does not contain `TEST_DUMMY_RPST`.
    - Normal debug capture does not contain `Authorization:`.
    - Normal debug capture does not contain `ST$`.
    - Secure debug capture is separate from normal debug capture.
  </acceptance_criteria>
  <done>Harness can detect accidental leakage of OCI signing material through normal debug helpers.</done>
</task>

<task type="auto">
  <name>Task 4: Run full auth/security suite</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, .github/workflows/shellcheck.yml</read_first>
  <action>Run the full mocked harness and ShellCheck. Confirm live OCI DNS smoke remains absent from the automated gate. Add a short note to the summary that optional live smoke can later be run through acmetest with `TEST_DNS=dns_oci`, `TestingDomain=<sacrificial-domain>`, and `CASE=le_test_dnsapi`, but Phase 1 completion is mocked only.</action>
  <verify>sh test/dns_oci_mock.sh && shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `sh test/dns_oci_mock.sh` exits 0.
    - `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh` exits 0.
    - No Phase 1 verification command requires `TEST_DNS=dns_oci` or `TestingDomain`.
    - Summary records live OCI DNS smoke as deferred UAT evidence, not the automated gate.
  </acceptance_criteria>
  <done>Auth/security harness is green and Phase 1 remains credential-free.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_auth_api_key,le_test_oci_auth_missing,le_test_oci_auth_resource_principal_current_state,le_test_oci_secure_debug_boundaries sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`
</verification>

<success_criteria>
- TEST-02 is represented by executable mocked auth/security cases.
- The harness clearly distinguishes current resource-principal fallback absence from future fallback-success proof.
- No automated Phase 1 command requires real OCI credentials, live DNS, or a sacrificial domain.
- D-04, D-07, D-10, and D-11 are visible in the implemented tests and summary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oci-hook-characterization-and-test-harness/01-03-SUMMARY.md`.
</output>

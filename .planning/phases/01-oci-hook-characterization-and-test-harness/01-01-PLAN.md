---
phase: 01-oci-hook-characterization-and-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/dns_oci_mock.sh]
autonomous: true
requirements: [TEST-01, TEST-02]
user_setup: []
must_haves:
  truths:
    - D-01 repo-local acmetest-style shell harness exists.
    - D-02 harness can later be run from acmetest with RUN_SCRIPT=acme.sh/test/dns_oci_mock.sh.
    - D-03 harness uses le_test_* cases, CASE selection, and no package manager.
    - D-05 harness can source acme.sh and dnsapi/dns_oci.sh while stubbing provider-boundary helpers.
  artifacts:
    - test/dns_oci_mock.sh
  key_links:
    - test/dns_oci_mock.sh sources ./acme.sh and ./dnsapi/dns_oci.sh.
    - test/dns_oci_mock.sh has a le_test_oci_harness_bootstrap case.
---

<objective>
Build the minimal repo-local OCI DNS hook shell harness.

Purpose: Give later OCI hook changes a deterministic proof surface before any delegated-zone or resource-principal behavior changes.
Output: `test/dns_oci_mock.sh` with acmetest-style runner, safe `acme.sh` sourcing, provider-boundary stubs, and a bootstrap case.
</objective>

<threat_model>
T-01-01: Harness accidentally executes real acme.sh CLI behavior while sourcing.
Mitigation: Source `acme.sh` only after setting positional parameters to a no-op function, then restore the original test arguments.

T-01-02: Harness accidentally reaches live OCI DNS or real credentials.
Mitigation: Override `_signed_request` and config helpers before running any `dns_oci_*` case; bootstrap must prove no live request was attempted.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-CONTEXT.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-RESEARCH.md
@.planning/phases/01-oci-hook-characterization-and-test-harness/01-PATTERNS.md
@acme.sh
@dnsapi/dns_oci.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acmetest-shaped harness skeleton</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>.planning/phases/01-oci-hook-characterization-and-test-harness/01-RESEARCH.md, .planning/phases/01-oci-hook-characterization-and-test-harness/01-PATTERNS.md, acme.sh, dnsapi/dns_oci.sh</read_first>
  <action>Create `test/dns_oci_mock.sh` as a POSIX `#!/usr/bin/env sh` script. Add assertion helpers named `_assert_eq`, `_assert_contains`, `_assert_not_contains`, `_assert_success`, and `_assert_failure`. Add an acmetest-style runner that discovers `le_test_*` functions from the script itself, supports `CASE` as a comma-separated selector, accepts `$1` as a case selector, and exits non-zero if any selected case fails. Do not use Bats, Python, Node, arrays, or Bash-only syntax.</action>
  <verify>CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `test/dns_oci_mock.sh` starts with `#!/usr/bin/env sh`.
    - `test/dns_oci_mock.sh` contains `le_test_oci_harness_bootstrap()`.
    - `test/dns_oci_mock.sh` contains `CASE="${1:-${CASE:-}}"` or equivalent `$1`/`CASE` selector handling.
    - `CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh` exits 0 after the bootstrap case is implemented.
  </acceptance_criteria>
  <done>Harness file exists, is POSIX-shell runnable, and has acmetest-style case selection.</done>
</task>

<task type="auto">
  <name>Task 2: Add safe source and mock reset helpers</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, acme.sh, dnsapi/dns_oci.sh</read_first>
  <action>Add a `_load_oci_hook_under_test` helper that saves the current positional arguments, defines `__dns_oci_test_noop() { :; }`, temporarily runs `set -- __dns_oci_test_noop`, sources `./acme.sh`, restores the saved arguments, then sources `./dnsapi/dns_oci.sh`. Add `_reset_oci_mocks` to clear mock captures and set dummy key-based OCI values: `OCI_CLI_TENANCY=ocid1.tenancy.oc1..test`, `OCI_CLI_USER=ocid1.user.oc1..test`, `OCI_CLI_REGION=us-ashburn-1`, and a multi-line `OCI_CLI_KEY` value. Add stub helpers for `_signed_request`, `_readaccountconf_mutable`, `_saveaccountconf_mutable`, `_clearaccountconf_mutable`, `_readini`, `_debug*`, and `_secure_debug*` that capture calls without touching real files or network.</action>
  <verify>CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `_load_oci_hook_under_test` sources `./acme.sh` with a no-op positional guard before sourcing `./dnsapi/dns_oci.sh`.
    - `_reset_oci_mocks` unsets or resets `MOCK_SIGNED_REQUESTS`, `MOCK_SAVED_KEYS`, `MOCK_CLEARED_KEYS`, `MOCK_DEBUG_LOG`, and `MOCK_SECURE_DEBUG_LOG`.
    - The default `_signed_request` stub records `method|target|body|return_field` and does not call `_get`, `_post`, `curl`, `wget`, or OCI endpoints.
  </acceptance_criteria>
  <done>Harness can load the real helper surface and replace external boundaries deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Prove bootstrap safety</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</read_first>
  <action>Implement `le_test_oci_harness_bootstrap` so it calls `_reset_oci_mocks`, proves `dns_oci_add`, `dns_oci_rm`, `_get_oci_zone`, `_oci_config`, and `_get_zone` are available as shell functions, and proves no signed request was recorded during bootstrap. Keep the case independent of any real OCI config, `$HOME/.oci/config`, RPST file, or private key file.</action>
  <verify>CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh` exits 0.
    - The bootstrap case asserts that `MOCK_SIGNED_REQUESTS` is empty after sourcing.
    - Running `sh test/dns_oci_mock.sh no_such_case` exits non-zero or reports no selected case, rather than silently passing.
  </acceptance_criteria>
  <done>Bootstrap proof is green and the harness is ready for behavior cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_harness_bootstrap sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh`
</verification>

<success_criteria>
- `test/dns_oci_mock.sh` exists and follows the acmetest-compatible shell shape.
- The harness sources the real `acme.sh` and `dnsapi/dns_oci.sh` without executing live CLI behavior.
- Provider-boundary and config/debug helpers are stubbed and resettable.
- D-01, D-02, D-03, and D-05 are visible in the implemented harness.
</success_criteria>

<output>
After completion, create `.planning/phases/01-oci-hook-characterization-and-test-harness/01-01-SUMMARY.md`.
</output>

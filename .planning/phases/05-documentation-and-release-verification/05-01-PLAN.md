---
phase: 05-documentation-and-release-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [dnsapi/dns_oci.sh, test/dns_oci_mock.sh]
autonomous: true
requirements: [DOC-01]
user_setup: []
must_haves:
  truths:
    - D-01 provider metadata must describe API-key auth, RP fallback, delegated subzones, and policy expectations.
    - D-06 API-key auth remains primary; RP auth is fallback only when key-based auth is unavailable/incomplete.
    - D-07 resource-principal support is scoped to `OCI_RESOURCE_PRINCIPAL_VERSION=2.2`.
    - D-08 required RP values are RPST, private PEM, and region; passphrase is optional.
    - D-09 RPST/private PEM/passphrase values use path-first inline fallback.
    - D-11 RP secrets and Authorization material are not persisted or normal-logged.
    - Stale comments saying encrypted private key passphrases are unsupported or RP signing is unimplemented must be removed or corrected.
  artifacts:
    - dnsapi/dns_oci.sh
    - test/dns_oci_mock.sh
  key_links:
    - `dns_oci_info` is the in-repo provider metadata surface.
    - `test/dns_oci_mock.sh` is the local regression surface for metadata drift.
---

<objective>
Update OCI provider metadata and stale hook/test prose so the in-repo provider
surface matches the behavior implemented in Phases 2-4.

Purpose: Make `dnsapi/dns_oci.sh` truthful for users and maintainers before
publishing wiki guidance or declaring release readiness.
Output: Updated `dns_oci_info`, corrected stale comments/descriptions, and a
mock harness metadata regression.
</objective>

<threat_model>
T-05-01: Provider metadata could still imply API-key-only behavior or omit
resource-principal fallback, causing OCI-hosted automation users to configure
the wrong auth path.
Mitigation: Metadata must list both API-key and RP variables and state auth
precedence plainly.

T-05-02: Stale comments could say passphrase-backed private keys or RP signing
are unsupported after Phase 4 implemented and tested them.
Mitigation: Remove or reword stale comments and test descriptions; add a test
that fails if the stale phrases return.

T-05-03: Policy guidance in metadata could overpromise universal tenancy access.
Mitigation: Keep metadata concise and point users to the wiki-ready guide for
adaptable policy examples.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-documentation-and-release-verification/05-CONTEXT.md
@.planning/phases/05-documentation-and-release-verification/05-RESEARCH.md
@.planning/phases/05-documentation-and-release-verification/05-PATTERNS.md
@.planning/phases/05-documentation-and-release-verification/05-VALIDATION.md
@.planning/phases/04-resource-principal-signing/04-CONTEXT.md
@.planning/phases/04-resource-principal-signing/04-VERIFICATION.md
@dnsapi/dns_oci.sh
@test/dns_oci_mock.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider metadata regression coverage</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/05-documentation-and-release-verification/05-PATTERNS.md</read_first>
  <behavior>
    - Test 1: `dns_oci_info` documents API-key auth and resource-principal fallback.
    - Test 2: metadata documents delegated-subzone or most-specific-zone behavior.
    - Test 3: metadata lists required and optional `OCI_RESOURCE_PRINCIPAL_*` variables.
    - Test 4: stale phrases such as `passphrase is not supported` and `signing is not implemented` are absent from changed hook/test prose.
  </behavior>
  <action>Add a case named `le_test_oci_provider_metadata_documents_current_behavior`. Source the hook through the existing harness and assert against `$dns_oci_info` plus the current source text. Use stable, case-sensitive substrings that reflect the intended user contract: `API-key`, `resource principal`, `fallback`, `delegated`, `subzone`, `OCI_RESOURCE_PRINCIPAL_VERSION`, `OCI_RESOURCE_PRINCIPAL_RPST`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM`, `OCI_RESOURCE_PRINCIPAL_REGION`, and `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM_PASSPHRASE`. Avoid asserting a full paragraph.</action>
  <verify>
    <automated>CASE=le_test_oci_provider_metadata_documents_current_behavior sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - The new metadata test fails against the old metadata.
    - The test is POSIX shell compatible and follows existing harness assertion helpers.
    - The test does not require live OCI config, RPST files, private PEM files, or network access.
  </acceptance_criteria>
  <done>Metadata drift has a focused local regression case.</done>
</task>

<task type="auto">
  <name>Task 2: Update `dns_oci_info` and hook comments</name>
  <files>dnsapi/dns_oci.sh</files>
  <read_first>dnsapi/dns_oci.sh, .planning/phases/05-documentation-and-release-verification/05-RESEARCH.md, .planning/phases/04-resource-principal-signing/04-VERIFICATION.md</read_first>
  <action>Update the provider metadata block so it states: complete OCI CLI or `OCI_CLI_*` API-key auth is primary; resource-principal auth is used as fallback when key-based auth is unavailable and RP v2.2 env is complete; delegated subzones are supported through most-specific accessible zone selection; DNS policy must allow zone read and TXT record mutation. Add metadata options for `OCI_RESOURCE_PRINCIPAL_VERSION`, `OCI_RESOURCE_PRINCIPAL_RPST`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM`, `OCI_RESOURCE_PRINCIPAL_REGION`, and optional `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM_PASSPHRASE`. Preserve the existing single-quoted multi-line `dns_oci_info` shape, avoid apostrophes inside metadata text, and make the Task 1 substrings literal so the metadata regression is meaningful. Remove or replace the stale encrypted-key passphrase unsupported note. Keep the metadata concise and keep the wiki URL unchanged.</action>
  <verify>
    <automated>CASE=le_test_oci_provider_metadata_documents_current_behavior sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - `dns_oci_info` describes current delegated-zone behavior.
    - `dns_oci_info` describes API-key-first and resource-principal-fallback auth order.
    - `dns_oci_info` lists the supported RP env names and optional passphrase env name.
    - Hook comments no longer claim passphrase-backed keys are unsupported.
    - No runtime behavior changes are introduced.
  </acceptance_criteria>
  <done>OCI provider metadata is current and truthful.</done>
</task>

<task type="auto">
  <name>Task 3: Correct stale Phase 3/4 test prose</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, .planning/phases/04-resource-principal-signing/04-VERIFICATION.md</read_first>
  <action>Reword or rename stale test descriptions/comments left from the Phase 3 signing-boundary characterization. Update the mock `_signed_request` resource-principal guard and the matching assertions together: the current stub emits "resource principal signing is not implemented yet" and tests assert "signing is not implemented"; replace that pair with a current stable diagnostic such as "resource principal signing failed" while preserving the `_oci_auth_mode=resource_principal` and no-PATCH checks. Preserve the behavioral checks that still matter: missing or unreadable RP material fails before PATCH, resource-principal values are not persisted, saved API-key config remains saved, and complete API-key auth wins over RP env. Do not delete useful regression coverage just because the old description is stale.</action>
  <verify>
    <automated>! rg -n "passphrase is not supported|signing is not implemented|before Phase 3/4 fallback|Phase 3 signing boundary" dnsapi/dns_oci.sh test/dns_oci_mock.sh</automated>
    <automated>sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - Current test prose no longer contradicts Phase 4 implementation.
    - Existing RP no-persistence and fallback-safety assertions remain covered.
    - Full mocked suite still exits 0.
  </acceptance_criteria>
  <done>Test/support prose matches current behavior without losing coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_provider_metadata_documents_current_behavior sh test/dns_oci_mock.sh`
- [ ] `! rg -n "passphrase is not supported|signing is not implemented|before Phase 3/4 fallback|Phase 3 signing boundary" dnsapi/dns_oci.sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
</verification>

<success_criteria>
- DOC-01 is covered for provider metadata.
- Metadata and comments accurately describe Phase 2-4 behavior.
- Stale unsupported/unimplemented prose is removed from changed hook/test files.
- No runtime auth, signing, zone discovery, or TXT payload behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-release-verification/05-01-SUMMARY.md`.
</output>

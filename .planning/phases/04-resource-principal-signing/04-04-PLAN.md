---
phase: 04-resource-principal-signing
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [test/dns_oci_mock.sh, dnsapi/dns_oci.sh]
autonomous: true
requirements: [RP-01, RP-02, RP-03, RP-04]
user_setup: []
must_haves:
  truths:
    - D-14 passphrase failure diagnostics name env vars and failure class only.
    - D-15 mocked exact header/request proof is sufficient for Phase 4.
    - D-16 full tests assert ST keyId, signed headers, signing strings, body hash/length, GET/PATCH flow, no persistence, and clean normal logs.
    - D-17 inline and path-backed material are both covered, including passphrase behavior.
    - D-19 passphrase success and failure are covered.
    - Static validation uses current local ShellCheck 0.11.0 and shfmt 3.13.1, both checked against latest releases during research.
  artifacts:
    - dnsapi/dns_oci.sh
    - test/dns_oci_mock.sh
  key_links:
    - passphrase signing stays in the OCI hook's OpenSSL temp-file pattern.
    - full `sh test/dns_oci_mock.sh` covers Phase 1-4 behavior.
    - ShellCheck and shfmt gates run before phase closeout.
---

<objective>
Complete passphrase coverage and run the full Phase 4 regression/static proof.

Purpose: Close the resource-principal signing phase with both encrypted-key
behavior and whole-hook confidence before documentation/release verification.
Output: passphrase success/failure tests, any minimal passphrase signing helper
fixes, full mocked suite, ShellCheck, and shfmt evidence.
</objective>

<threat_model>
T-04-10: Encrypted RP private keys could fail or leak passphrases through normal
OpenSSL diagnostics.
Mitigation: Add success and failure tests; pass failure through hook-owned
env-name-only diagnostics.

T-04-11: Focused signer tests could pass while older zone, TXT, API-key, or auth
selection behavior regresses.
Mitigation: Run the full mock suite plus ShellCheck and shfmt after the final
passphrase changes.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-resource-principal-signing/04-CONTEXT.md
@.planning/phases/04-resource-principal-signing/04-RESEARCH.md
@.planning/phases/04-resource-principal-signing/04-PATTERNS.md
@.planning/phases/04-resource-principal-signing/04-VALIDATION.md
@.planning/phases/04-resource-principal-signing/04-03-SUMMARY.md
@dnsapi/dns_oci.sh
@test/dns_oci_mock.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add passphrase success and failure tests</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/04-resource-principal-signing/04-RESEARCH.md</read_first>
  <behavior>
    - Test 1: passphrase-backed RP private PEM signs successfully through the mocked signer path.
    - Test 2: wrong or missing passphrase fails before PATCH with env-name-only diagnostics.
    - Test 3: normal and secure logs do not contain the passphrase value.
  </behavior>
  <action>Add cases named `le_test_oci_rp_passphrase_success` and `le_test_oci_rp_passphrase_failure_is_clean`. Prefer stubbing the local passphrase-capable signing helper so the tests prove hook behavior without requiring a brittle encrypted-key fixture. The success case must assert the helper receives passphrase content through a file or non-argv path, produces `TEST_PASSPHRASE_SIGNATURE`, and lets public add reach PATCH. The failure case must make the helper fail and assert the hook returns non-zero before PATCH, error logs contain `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM_PASSPHRASE` and `signing failed` or equivalent failure class, and normal/secure logs do not contain `TEST_INLINE_PASSPHRASE` or a passphrase path. If a real encrypted-key fixture is added instead, keep it generated or static inside the mock harness and do not require live OCI material.</action>
  <verify>
    <automated>CASE=le_test_oci_rp_passphrase_success,le_test_oci_rp_passphrase_failure_is_clean sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - Passphrase-backed signing can succeed.
    - Missing/wrong passphrase fails before PATCH.
    - Normal error text names `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM_PASSPHRASE`.
    - No normal or secure log contains passphrase value or passphrase path.
  </acceptance_criteria>
  <done>Passphrase behavior is covered before final validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement or tighten passphrase signing helper</name>
  <files>dnsapi/dns_oci.sh</files>
  <read_first>dnsapi/dns_oci.sh, test/dns_oci_mock.sh, .planning/phases/04-resource-principal-signing/04-PATTERNS.md</read_first>
  <behavior>
    - Test 1: no-passphrase RP signing still delegates to the existing `_sign` path.
    - Test 2: passphrase RP signing uses a passphrase-safe OpenSSL path.
    - Test 3: signing failure returns clean hook-owned diagnostics.
  </behavior>
  <action>If Plan 04-02 did not already complete passphrase signing, add or tighten `_oci_sign_with_private_key_file`. With no passphrase, delegate to `_sign "$keyfile" sha256`. With a passphrase, write passphrase content to a temp file, run `${ACME_OPENSSL_BIN:-openssl} dgst -sha256 -sign "$keyfile" -passin file:"$passphrase_file"` and pipe binary output to `_base64`, then remove the passphrase temp file. Avoid putting the passphrase value in command arguments, environment variables, normal logs, or secure logs. On failure, set the RP auth/signing failure flag and emit an env-name-only `_err` naming `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM_PASSPHRASE` and the signing failure class. Ensure temp key and passphrase files are removed on every return path.</action>
  <verify>
    <automated>CASE=le_test_oci_rp_passphrase_success,le_test_oci_rp_passphrase_failure_is_clean sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - No-passphrase signing path remains compatible with `_sign`.
    - Passphrase path uses OpenSSL `-passin` without exposing the passphrase in argv.
    - Temp key and passphrase files are removed on success and failure.
    - Passphrase failures are clean and terminal.
  </acceptance_criteria>
  <done>Passphrase support is implemented with the existing OpenSSL pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 4 regression and static gates</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/04-resource-principal-signing/04-VALIDATION.md</read_first>
  <action>Run the full mocked suite and repair only Phase 4 regressions in `test/dns_oci_mock.sh` or `dnsapi/dns_oci.sh`. Required Phase 4 cases include `le_test_oci_rp_loads_inline_material`, `le_test_oci_rp_loads_path_material`, `le_test_oci_rp_missing_material_reports_env_names`, `le_test_oci_rp_unsupported_version_reports_env_name`, `le_test_oci_rp_signs_get_with_st_key_id`, `le_test_oci_rp_signs_patch_body_headers`, `le_test_oci_rp_refreshes_path_material_between_requests`, `le_test_oci_rp_does_not_persist_or_normal_log_material`, `le_test_oci_rp_signing_failure_stops_zone_fallback`, `le_test_oci_rp_passphrase_success`, and `le_test_oci_rp_passphrase_failure_is_clean`. Then run ShellCheck and shfmt exactly as listed below. Research checked current local versions against upstream releases on 2026-05-15; do not install or update tooling during execution.</action>
  <verify>
    <automated>sh test/dns_oci_mock.sh</automated>
    <automated>shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh</automated>
    <automated>shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh && git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh test/dns_oci_mock.sh` exits 0 and includes all required Phase 4 cases.
    - Existing Phase 1 zone harness, Phase 2 delegated-zone/TXT, and Phase 3 auth-selection cases still pass.
    - ShellCheck exits 0 for both touched shell files.
    - shfmt exits 0 and leaves no formatting diff.
    - No new runtime dependency, SDK, or package manager is added.
  </acceptance_criteria>
  <done>Full mocked and static validation is green for Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_rp_passphrase_success,le_test_oci_rp_passphrase_failure_is_clean sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`
- [ ] `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh && git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh`
</verification>

<success_criteria>
- RP-01 is covered for inline, path-backed, and passphrase material.
- RP-02 is covered for GET and PATCH signing.
- RP-03 is covered by no persistence through the public path.
- RP-04 is covered by clean normal logs and secure-debug-only signing internals.
- Full mock, ShellCheck, and shfmt gates pass before Phase 4 verification.
</success_criteria>

<output>
After completion, create `.planning/phases/04-resource-principal-signing/04-04-SUMMARY.md`.
</output>

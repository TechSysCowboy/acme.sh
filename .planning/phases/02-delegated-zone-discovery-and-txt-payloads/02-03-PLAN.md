---
phase: 02-delegated-zone-discovery-and-txt-payloads
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [test/dns_oci_mock.sh, dnsapi/dns_oci.sh]
autonomous: true
requirements: [ZONE-02, ZONE-01, ZONE-03, TXT-01, TXT-02, TXT-03]
user_setup: []
must_haves:
  truths:
    - D-15 successful add/remove normal output preserves current success text.
    - D-16 PATCH failure permission hints stay as-is.
    - D-17 no-zone failure includes one extra hint line.
    - D-18 debug contains factual probe/selection details without secrets.
    - D-19 logging level choices respect _debug2 and _debug3 sensitivity rules.
    - D-21 public behavior leads proof; helper assertions are only for indirect facts.
    - D-22 required matrix covers parent/apex, delegated subzone, fallback, no-zone, apex wildcard, and delegated wildcard.
    - D-23 debug assertions avoid exact OCI-controlled message phrasing.
    - D-24 mock status/body/id signal remains focused and portable.
    - D-25 escaping tests stay separate focused cases.
    - D-26 no-zone and hard-fail authorization prove no PATCH.
  artifacts:
    - test/dns_oci_mock.sh
    - dnsapi/dns_oci.sh
  key_links:
    - Full `sh test/dns_oci_mock.sh` suite covers all Phase 2 requirements.
    - ShellCheck runs against both touched shell files.
    - shfmt is recorded if unavailable locally.
---

<objective>
Complete the mocked Phase 2 proof matrix, verify operator-facing/debug behavior,
and run the final local shell validation gates.

Purpose: Make Phase 2 execution close with deterministic evidence that delegated
subzones, wildcard names, fallback, no-zone safety, debug facts, and payload
escaping are all covered without live OCI credentials.
Output: Passing full `test/dns_oci_mock.sh` suite, ShellCheck proof, and summary
of any local shfmt availability gap.
</objective>

<threat_model>
T-02-07: A later test addition could accidentally rely on exact OCI error text
and become brittle.
Mitigation: Assert stable hook-controlled facts such as status/code fields,
candidate zone, selected zone, fallback decision, and no-PATCH captures.

T-02-08: Final verification could pass despite missing one Phase 2 requirement.
Mitigation: Run the full wildcard-explicit matrix and include every Phase 2
requirement in this plan's verification and success criteria.

T-02-09: Debug verification could miss secret leakage.
Mitigation: Re-run secure-debug boundary expectations and ensure routine probe
facts do not require `_debug3` or normal output secrets.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-CONTEXT.md
@.planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-RESEARCH.md
@.planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-PATTERNS.md
@.planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-VALIDATION.md
@test/dns_oci_mock.sh
@dnsapi/dns_oci.sh
@.github/workflows/shellcheck.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add final debug and normal-output assertions</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-CONTEXT.md</read_first>
  <action>Add or tighten assertions in existing Phase 2 cases so successful add/remove normal output remains the current success shape, PATCH failure permission hints remain present, no-zone failure includes the extra zone-existence/read-permission hint, and debug captures factual probe/selection details without asserting exact OCI-owned message text. Assertions may check stable substrings such as candidate zone names, numeric status/code fields, selected zone names, and absence of `Authorization:` in normal debug captures.</action>
  <verify>CASE=le_test_oci_lookup_ambiguous_404_falls_back,le_test_oci_lookup_visible_authz_fails_hard,le_test_oci_no_zone_failure sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - Successful add/remove cases still assert current success messages.
    - No-zone case asserts the added hint line about zone existence and read permission.
    - Debug assertions do not compare full OCI error prose.
    - Normal debug captures do not contain `Authorization:`, dummy private key text, or RPST-like token text.
  </acceptance_criteria>
  <done>Operator-facing and debug behavior is covered without brittle OCI prose matching.</done>
</task>

<task type="auto">
  <name>Task 2: Run wildcard-explicit full matrix</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-VALIDATION.md</read_first>
  <action>Run the full Phase 2 matrix and repair any failing test or code behavior within `test/dns_oci_mock.sh` and `dnsapi/dns_oci.sh`. The required matrix is parent/apex, delegated subzone, fallback to parent, no-zone, apex wildcard, delegated wildcard, ambiguous 404 fallback, visible authz hard-fail, TXT value escaping, and record domain escaping.</action>
  <verify>sh test/dns_oci_mock.sh</verify>
  <acceptance_criteria>
    - `sh test/dns_oci_mock.sh` exits 0.
    - Full suite output includes all Phase 2 cases: `le_test_oci_lookup_ambiguous_404_falls_back`, `le_test_oci_lookup_visible_authz_fails_hard`, `le_test_oci_apex_wildcard_add`, `le_test_oci_delegated_wildcard_add`, `le_test_oci_txt_value_json_escape`, and `le_test_oci_record_domain_json_escape`.
    - Existing Phase 1 auth and secure-debug cases still pass.
    - No test requires real OCI DNS, real OCI config, real key files, or live resource-principal files.
  </acceptance_criteria>
  <done>Full mocked proof suite is green and covers all Phase 2 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Run static validation and record formatter availability</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .github/workflows/shellcheck.yml, .planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-RESEARCH.md</read_first>
  <action>Run ShellCheck against touched shell files with the same local exemptions used in Phase 1: `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`. If `shfmt` is available, run `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh` and confirm no formatting diff remains. If `shfmt` is absent, record that absence in the summary and do not install or update formatter dependencies during Phase 2.</action>
  <verify>shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh</verify>
  <acceptance_criteria>
    - ShellCheck command exits 0.
    - If shfmt exists, `git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh` exits 0 after formatting.
    - If shfmt is absent, `02-03-SUMMARY.md` records local shfmt absence and notes formatter dependency/pin cleanup belongs to Phase 5.
    - `dnsapi/dns_oci.sh` remains POSIX shell compatible and does not add SDK or package-manager dependencies.
  </acceptance_criteria>
  <done>Touched shell files pass available static gates and formatter availability is truthfully recorded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`
- [ ] If `shfmt` exists: `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh && git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh`
- [ ] If `shfmt` is absent: record the absence in `02-03-SUMMARY.md`
</verification>

<success_criteria>
- All Phase 2 requirements ZONE-01, ZONE-02, ZONE-03, TXT-01, TXT-02, and TXT-03 are covered by passing mocked tests.
- Full local `sh test/dns_oci_mock.sh` exits 0.
- ShellCheck exits 0 for `test/dns_oci_mock.sh` and `dnsapi/dns_oci.sh`.
- Normal output/debug behavior matches the Phase 2 context decisions.
- No live OCI credentials or live OCI calls are required.
</success_criteria>

<output>
After completion, create `.planning/phases/02-delegated-zone-discovery-and-txt-payloads/02-03-SUMMARY.md`.
</output>

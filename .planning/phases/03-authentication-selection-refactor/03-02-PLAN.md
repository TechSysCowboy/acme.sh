---
phase: 03-authentication-selection-refactor
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [test/dns_oci_mock.sh, dnsapi/dns_oci.sh]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - D-10 saved OCI_CLI account config survives RP fallback runs.
    - D-11 no RP version, region, token, key path, token, key, or detection flag is saved.
    - D-12 key-path failure diagnostics remain process-local.
    - D-13 complete API-key config wins over complete RP env.
    - D-14 API-key success, partial-key RP boundary, missing-all-auth, and API-key-wins cases are all covered.
    - D-17 diagnostic assertions use stable substrings.
  artifacts:
    - dnsapi/dns_oci.sh
    - test/dns_oci_mock.sh
  key_links:
    - `_oci_config` persistence rules remain isolated to durable API-key values.
    - RP detector reads environment only and has no account-config side effects.
    - Tests prove API-key-wins and no-RP-persistence behavior through public calls.
---

<objective>
Preserve current OCI CLI / `OCI_CLI_*` persistence semantics while proving
resource-principal detection remains process-local.

Purpose: Ensure the selector refactor does not corrupt existing saved API-key
configuration and does not introduce persistent RP secrets or metadata.
Output: Focused persistence tests and any selector refinements needed to keep
API-key account config durable and RP values ephemeral.
</objective>

<threat_model>
T-03-04: Falling back to RP could clear or overwrite saved API-key config and
break later API-key runs.
Mitigation: Tests must seed/read saved API-key values and prove RP fallback does
not clear `OCI_CLI_*` keys.

T-03-05: RP env variables could be persisted accidentally through existing
account config helpers.
Mitigation: RP detector must never call `_saveaccountconf_mutable` or
`_clearaccountconf_mutable` for `OCI_RESOURCE_PRINCIPAL_*`, and tests assert the
capture files contain no RP keys.

T-03-06: Complete API-key plus complete RP env could expose RP presence in
normal logs or select the wrong path.
Mitigation: API-key-wins test must assert PATCH success, durable API-key saves,
no RP persistence, and no normal debug/info RP presence.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-authentication-selection-refactor/03-CONTEXT.md
@.planning/phases/03-authentication-selection-refactor/03-RESEARCH.md
@.planning/phases/03-authentication-selection-refactor/03-PATTERNS.md
@.planning/phases/03-authentication-selection-refactor/03-VALIDATION.md
@dnsapi/dns_oci.sh
@test/dns_oci_mock.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove API-key wins over complete RP env</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Complete API-key config and complete RP env still select API-key auth and reach PATCH.
    - Test 2: Normal debug/info output does not mention RP presence on the API-key-winning path.
  </behavior>
  <action>Add a case named `le_test_oci_auth_api_key_wins_over_resource_principal`. Per D-13, use complete default API-key env from `_reset_oci_mocks` and also set complete RP env values. Call `dns_oci_add "_acme-challenge.www.example.com" "api-key-wins-value"`. Assert PATCH target `/20180115/zones/example.com/records`, assert `_oci_auth_mode=api_key` if the mode global is inspectable after the call, assert `OCI_CLI_TENANCY`, `OCI_CLI_USER`, `OCI_CLI_REGION`, and `OCI_CLI_KEY` are saved as before, and assert no `OCI_RESOURCE_PRINCIPAL` substring appears in `MOCK_SAVED_KEYS`, `MOCK_CLEARED_KEYS`, `MOCK_DEBUG_LOG`, or `MOCK_INFO_LOG`.</action>
  <verify>
    <automated>CASE=le_test_oci_auth_api_key_wins_over_resource_principal sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - Complete API-key config wins when RP env is also complete.
    - The public add call reaches the mocked PATCH path.
    - API-key persistence assertions match the existing `le_test_oci_auth_api_key` behavior.
    - Normal logs and account-config captures contain no RP presence.
  </acceptance_criteria>
  <done>API-key primary behavior is protected against RP-env precedence regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove OCI CLI config and RP persistence boundaries</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-PATTERNS.md</read_first>
  <behavior>
    - Test 1: OCI CLI config file values still configure API-key auth and reach PATCH.
    - Test 2: Resource-principal-only boundary leaves `MOCK_SAVED_KEYS` and `MOCK_CLEARED_KEYS` free of all RP variable names and fixture values.
  </behavior>
  <action>Add a case named `le_test_oci_auth_oci_cli_config_file_primary` that proves AUTH-01's OCI CLI config path remains primary. Extend the `_readini` mock only as needed to return fixture values for a mock `DEFAULT` profile: tenancy, user, region, and key_file. Create a temp key file under `$_DNS_OCI_MOCK_DIR`, set `OCI_CLI_CONFIG_FILE` to a temp config path, unset direct `OCI_CLI_TENANCY`, `OCI_CLI_USER`, `OCI_CLI_REGION`, `OCI_CLI_KEY`, and `OCI_CLI_KEY_FILE`, then call public `dns_oci_add` and assert it reaches PATCH and records `readini` lookups for tenancy, user, region, and key_file. Also add a case named `le_test_oci_auth_resource_principal_does_not_persist`, or extend `le_test_oci_auth_resource_principal_detected_boundary` if keeping one case is cleaner. Per D-10 and D-11, use explicit fixture values such as `OCI_RESOURCE_PRINCIPAL_RPST=/tmp/rpst.token`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM=/tmp/private.pem`, `OCI_RESOURCE_PRINCIPAL_REGION=us-ashburn-1`, and `OCI_RESOURCE_PRINCIPAL_VERSION=2.2`. Assert none of these variable names or values appears in `MOCK_SAVED_KEYS` or `MOCK_CLEARED_KEYS`. If implementation currently saves or clears RP names, remove those calls and keep RP detection environment-only.</action>
  <verify>
    <automated>CASE=le_test_oci_auth_oci_cli_config_file_primary,le_test_oci_auth_resource_principal_does_not_persist sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - OCI CLI config file tenancy, user, region, and key_file values can still configure API-key auth.
    - OCI CLI config file path reaches the mocked PATCH path through API-key mode.
    - Mock `readini` captures prove tenancy, user, region, and key_file were read from config.
    - No `OCI_RESOURCE_PRINCIPAL_` variable name appears in saved account config captures.
    - No RPST path, private PEM path, version value, or region fixture appears in saved account config captures.
    - No `OCI_RESOURCE_PRINCIPAL_` variable name appears in cleared account config captures.
    - API-key saved config behavior from `le_test_oci_auth_api_key` still passes.
  </acceptance_criteria>
  <done>OCI CLI config-file auth still works and RP selection remains process-local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten missing-all-auth diagnostics</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Missing key auth and missing RP env fails before PATCH.
    - Test 2: Error log names at least one key-based missing field and at least one RP missing field.
  </behavior>
  <action>Rename or add a case named `le_test_oci_auth_missing_reports_both_paths`. Per AUTH-03 and D-17, unset API-key values and RP values, call public `dns_oci_add`, and assert stable substrings for both paths: key-based missing pieces such as `OCI_CLI_TENANCY`, `OCI_CLI_USER`, or `OCI_CLI_KEY`, and RP missing pieces such as `OCI_RESOURCE_PRINCIPAL_VERSION`, `OCI_RESOURCE_PRINCIPAL_RPST`, `OCI_RESOURCE_PRINCIPAL_PRIVATE_PEM`, or `OCI_RESOURCE_PRINCIPAL_REGION`. Adjust selector diagnostics if needed so missing-all-auth reports both paths without exact full-message assertions. Assert no PATCH is captured and no RP names are persisted.</action>
  <verify>
    <automated>CASE=le_test_oci_auth_missing_reports_both_paths sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - Missing all auth fails before any PATCH request.
    - Error log distinguishes key-based configuration problems from RP configuration problems.
    - Assertions use stable variable-name or boundary substrings.
    - No RP values are saved or cleared in account config captures.
  </acceptance_criteria>
  <done>Missing auth diagnostics are actionable and distinguish both auth paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_auth_api_key_wins_over_resource_principal,le_test_oci_auth_resource_principal_does_not_persist sh test/dns_oci_mock.sh`
- [ ] `CASE=le_test_oci_auth_api_key,le_test_oci_auth_oci_cli_config_file_primary,le_test_oci_auth_missing_reports_both_paths sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
</verification>

<success_criteria>
- AUTH-01 remains true for both env and saved-config API-key flows.
- AUTH-02 remains true because RP is only selected after API-key config failure.
- AUTH-03 missing-all-auth diagnostics name both key-based and RP problems.
- RP values are never saved, cleared, or normal-logged.
- Complete API-key config wins over complete RP env.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-selection-refactor/03-02-SUMMARY.md`.
</output>

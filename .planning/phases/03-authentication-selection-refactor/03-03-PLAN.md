---
phase: 03-authentication-selection-refactor
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [test/dns_oci_mock.sh, dnsapi/dns_oci.sh]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - D-14 full public matrix covers API-key success, partial-key RP boundary, missing-all-auth, and API-key-wins behavior.
    - D-15 `dns_oci_add` leads the matrix and `dns_oci_rm` has a focused selector smoke case.
    - D-16 RP fallback tests prove failure before PATCH, mode fact, and no RP persistence.
    - D-17 diagnostics use stable substrings rather than exact full messages.
    - Phase 3 remains credential-free and live OCI validation stays deferred.
    - ShellCheck and shfmt are run with current local versions checked during research.
  artifacts:
    - dnsapi/dns_oci.sh
    - test/dns_oci_mock.sh
  key_links:
    - Full `sh test/dns_oci_mock.sh` includes all Phase 1, Phase 2, and Phase 3 mock cases.
    - ShellCheck validates both touched shell files.
    - shfmt validates formatting without introducing a new dependency.
---

<objective>
Complete the Phase 3 auth-selection proof matrix and static shell validation.

Purpose: Close planning around the behavior users will rely on before Phase 4:
API-key first, RP fallback only after key failure, explicit diagnostics, and no
RP persistence.
Output: Full mocked proof suite, focused remove-path auth selector smoke, and
static validation evidence for `dnsapi/dns_oci.sh` and `test/dns_oci_mock.sh`.
</objective>

<threat_model>
T-03-07: Add-only testing could miss that remove bypasses auth selection.
Mitigation: Add one focused `dns_oci_rm` selector smoke case, per D-15.

T-03-08: A final matrix could pass despite dropping an older Phase 1 or Phase 2
case.
Mitigation: Run the full `sh test/dns_oci_mock.sh` suite, not just Phase 3
cases.

T-03-09: Static validation could drift from current dependency versions.
Mitigation: Use local ShellCheck `0.11.0` and shfmt `3.13.1`, which were
checked against latest upstream releases during research.
</threat_model>

<execution_context>
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/Developer/Djelibeybi/acme.sh/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-authentication-selection-refactor/03-CONTEXT.md
@.planning/phases/03-authentication-selection-refactor/03-RESEARCH.md
@.planning/phases/03-authentication-selection-refactor/03-PATTERNS.md
@.planning/phases/03-authentication-selection-refactor/03-VALIDATION.md
@dnsapi/dns_oci.sh
@test/dns_oci_mock.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remove-path auth selector smoke</name>
  <files>test/dns_oci_mock.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `dns_oci_rm` with complete API-key config reaches mocked PATCH and keeps `_oci_auth_mode=api_key`.
    - Test 2: The remove smoke uses the same selected zone and does not change remove TTL behavior.
  </behavior>
  <action>Per D-15, add a focused case named `le_test_oci_rm_uses_auth_selector`. Use complete API-key config and `MOCK_OCI_ZONES=example.com`, then call `dns_oci_rm "_acme-challenge.www.example.com" "remove-selector-value"`. Assert `_oci_auth_mode=api_key` if inspectable, `PATCH|/20180115/zones/example.com/records`, `"operation":"REMOVE"`, no `"ttl":`, and no `OCI_RESOURCE_PRINCIPAL` in saved or normal log captures. Do not duplicate the whole add matrix for remove.</action>
  <verify>
    <automated>CASE=le_test_oci_rm_uses_auth_selector sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - Remove path goes through auth selection and reaches PATCH on API-key auth.
    - Remove payload remains `"operation":"REMOVE"` and still has no TTL.
    - No RP value is saved or normal-logged by the remove smoke.
  </acceptance_criteria>
  <done>Remove path has a focused auth-selection regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Run full auth-selection matrix</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-VALIDATION.md</read_first>
  <action>Run the full mocked suite and repair any Phase 3 regression within `test/dns_oci_mock.sh` or `dnsapi/dns_oci.sh`. The required Phase 3 cases are `le_test_oci_auth_api_key`, `le_test_oci_auth_resource_principal_detected_boundary`, `le_test_oci_auth_partial_key_falls_back_to_resource_principal`, `le_test_oci_auth_api_key_wins_over_resource_principal`, `le_test_oci_auth_resource_principal_does_not_persist`, `le_test_oci_auth_missing_reports_both_paths`, and `le_test_oci_rm_uses_auth_selector`. Per D-14 through D-17, the suite must prove API-key success, partial-key RP boundary, missing-all-auth, API-key-wins, add-led public proof, one remove smoke, RP mode/no-PATCH/no-persistence facts, and stable diagnostic substrings.</action>
  <verify>
    <automated>sh test/dns_oci_mock.sh</automated>
  </verify>
  <acceptance_criteria>
    - `sh test/dns_oci_mock.sh` exits 0.
    - Full suite output includes all required Phase 3 cases listed in this task.
    - Existing Phase 1 auth/security and Phase 2 zone/TXT cases still pass.
    - No test requires real OCI CLI config, live OCI DNS, RPST files, private PEM files, or live OCI-hosted execution.
  </acceptance_criteria>
  <done>Full mocked proof matrix is green and covers all Phase 3 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Run static validation and formatting gates</name>
  <files>test/dns_oci_mock.sh, dnsapi/dns_oci.sh</files>
  <read_first>test/dns_oci_mock.sh, dnsapi/dns_oci.sh, .planning/phases/03-authentication-selection-refactor/03-RESEARCH.md</read_first>
  <action>Run `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`. Run `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh` and then `git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh`. Research confirmed local ShellCheck `0.11.0` and shfmt `3.13.1` are current upstream latest as of 2026-05-15, so do not install or update formatter dependencies during execution. Repair only Phase 3-introduced lint/format issues.</action>
  <verify>
    <automated>shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh</automated>
  </verify>
  <acceptance_criteria>
    - ShellCheck exits 0 for both touched files.
    - `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh` exits 0.
    - `git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh` exits 0 after formatting.
    - No new runtime dependency, SDK, or package manager is added.
  </acceptance_criteria>
  <done>Touched shell files pass current local static and format gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `CASE=le_test_oci_rm_uses_auth_selector sh test/dns_oci_mock.sh`
- [ ] `sh test/dns_oci_mock.sh`
- [ ] `shellcheck -e SC2181 -e SC2089 test/dns_oci_mock.sh dnsapi/dns_oci.sh`
- [ ] `shfmt -l -w -i 2 test/dns_oci_mock.sh dnsapi/dns_oci.sh && git diff --exit-code -- test/dns_oci_mock.sh dnsapi/dns_oci.sh`
</verification>

<success_criteria>
- AUTH-01, AUTH-02, and AUTH-03 are all covered by passing mocked tests.
- Add path carries the full auth matrix and remove path has focused selector proof.
- RP fallback fails before signing or PATCH in Phase 3.
- API-key config remains primary and durable.
- RP values remain ephemeral and unpersisted.
- Full mocked suite, ShellCheck, and shfmt pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication-selection-refactor/03-03-SUMMARY.md`.
</output>
